---
apiVersion: v1
kind: Service
metadata:
  name: acme-gateway-external
  namespace: nginx-gateway
  labels:
    app.kubernetes.io/instance: nginx-gateway
    app.kubernetes.io/managed-by: nginx-gateway-nginx
    app.kubernetes.io/name: acme-gateway-nginx
    gateway.networking.k8s.io/gateway-name: acme-gateway
spec:
  type: ClusterIP
  externalIPs:
  - ${GATEWAY_EXTERNAL_IP}
  ports:
  - name: http
    port: 80
    protocol: TCP
    targetPort: 80
  selector:
    app.kubernetes.io/instance: nginx-gateway
    app.kubernetes.io/managed-by: nginx-gateway-nginx
    app.kubernetes.io/name: acme-gateway-nginx
    gateway.networking.k8s.io/gateway-name: acme-gateway
